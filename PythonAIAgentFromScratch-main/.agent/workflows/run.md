---
description: Start the AI Agent Server
---
1. Clean up any existing processes on port 5000
2. Install dependencies from requirements.txt
3. Start the Flask server

// turbo
4. Run the startup command:
```powershell
./START_AGENT.bat
```
